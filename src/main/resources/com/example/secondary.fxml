<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.SecondaryController"
            stylesheets="@dashboard.css"
            prefHeight="720.0" prefWidth="1280.0">

    <!-- MENU LATERAL NOVO -->
    <left>
        <VBox fx:id="sidebar" styleClass="sidebar">

            <!-- Header do Menu -->
            <HBox styleClass="sidebar-header">
                <Label fx:id="sidebarTitle" text="💈 AGS" styleClass="sidebar-title"/>
            </HBox>

            <!-- Itens do Menu -->
            <VBox fx:id="menuContainer" spacing="0">

                <!-- Clientes -->
                <HBox fx:id="menuClientes" styleClass="menu-item" onMouseClicked="#showClientes">
                    <Label fx:id="iconClientes" text="👥" styleClass="menu-icon"/>
                    <Label fx:id="textClientes" text="Clientes" styleClass="menu-text"/>
                </HBox>

                <!-- Agendamentos -->
                <HBox fx:id="menuAgendamentos" styleClass="menu-item" onMouseClicked="#showAgendamentos">
                    <Label fx:id="iconAgendamentos" text="📅" styleClass="menu-icon"/>
                    <Label fx:id="textAgendamentos" text="Agendamentos" styleClass="menu-text"/>
                </HBox>

                <!-- Serviços -->
                <HBox fx:id="menuServicos" styleClass="menu-item" onMouseClicked="#showServicos">
                    <Label fx:id="iconServicos" text="✂️" styleClass="menu-icon"/>
                    <Label fx:id="textServicos" text="Serviços" styleClass="menu-text"/>
                </HBox>

                <HBox fx:id="menuFinancas"
                      styleClass="menu-item"
                      onMouseClicked="#showFinancas">
                    <Label text="💰" styleClass="menu-icon"/>
                    <Label text="Finanças" styleClass="menu-text"/>
                </HBox>

                <HBox fx:id="menuUsuarios" styleClass="menu-item" onMouseClicked="#showUsuarios">
                    <Label fx:id="iconUsuarios" text="👤" styleClass="menu-icon"/>
                    <Label fx:id="textUsuarios" text="Usuários" styleClass="menu-text"/>
                </HBox>

            </VBox>

            <!-- Empurra logout para baixo -->
            <Region VBox.vgrow="ALWAYS"/>

            <!-- Logout -->
            <HBox fx:id="menuLogout" styleClass="logout-item" onMouseClicked="#handleLogout">
                <Label fx:id="iconLogout" text="🚪" styleClass="logout-icon"/>
                <Label fx:id="textLogout" text="Sair" styleClass="logout-text"/>
            </HBox>

        </VBox>
    </left>

    <!-- CONTEÚDO PRINCIPAL -->
    <center>
        <StackPane fx:id="conteudoArea" styleClass="content-area">
            <Label text="Selecione uma opção no menu"
                   styleClass="placeholder-text"/>
        </StackPane>
    </center>

</BorderPane>
